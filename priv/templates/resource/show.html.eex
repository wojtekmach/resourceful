<h2>Show <%= @resource.singular %></h2>

<ul>
<%= for {name, type} <- @resource.fields do %>
  <li>
    <strong><%= Phoenix.Naming.humanize(name)%>:</strong>
    <%= display @struct, name, type %>
  </li>
<% end %>
</ul>

<span><%= link "Edit", to: resource_path(@conn, @resource, [:edit, @struct]) %></span>
<span><%= link "Back", to: resource_path(@conn, @resource, [:index]) %></span>
