<h2>Edit <%= @resource.singular %></h2>

<%= render "form.html", Map.put(assigns, :action, resource_path(@conn, @resource, [:update, @struct])) %>

<span><%= link "Back", to: resource_path(@conn, @resource, [:index]) %></span>